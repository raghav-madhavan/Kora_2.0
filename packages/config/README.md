# `packages/config` (reserved)

Placeholder directory. Shared tooling configs live in dedicated packages instead:

- [`packages/eslint-config`](../eslint-config) — `@repo/eslint-config`
- [`packages/typescript-config`](../typescript-config) — `@repo/typescript-config`

This folder is kept for future shared config (e.g. Tailwind preset) if needed.
